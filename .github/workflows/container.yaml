name: Build and Push Container Image to GHCR

on:
  push:
    branches:
      - "main"
  schedule:
    - cron: "31 5 * * 1"
  workflow_dispatch:

jobs:
  docker:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        name: Check out code

      - name: Set flags
        id: set_flags
        run: |
          branch="${GITHUB_REF##*/}"

          if [ "$branch" = "main" ]; then
            echo "addLatest=true" >> $GITHUB_OUTPUT
          else
            echo "addLatest=false" >> $GITHUB_OUTPUT
          fi

          if [ "$GITHUB_EVENT_NAME" = "schedule" ]; then
            echo "addTimestamp=true" >> $GITHUB_OUTPUT
          else
            echo "addTimestamp=false" >> $GITHUB_OUTPUT
          fi

      - name: Build & push Docker image
        uses: mr-smithers-excellent/docker-build-push@ef432483bd0252dd948d1d11e60c96a1c93646a9 # v7.0
        with:
          image: hpilo-exporter
          registry: ghcr.io
          addLatest: ${{ steps.set_flags.outputs.addLatest }}
          addTimestamp:  ${{ steps.set_flags.outputs.addTimestamp }}
          enableBuildKit: true
          multiPlatform: true
          platform: linux/amd64,linux/arm64,linux/arm/v7
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

